name: Build RAMPilot for Windows
on:
  workflow_dispatch:
  push:
    paths:
      - '**.cs'
      - '**.xaml'
      - '**.csproj'
      - 'app.manifest'
      - '.github/workflows/build-windows.yml'
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      - name: Publish single-file EXE
        run: dotnet publish RAMPilot.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -o publish
      - name: Upload RAMPilot
        uses: actions/upload-artifact@v4
        with:
          name: RAMPilot-Windows-x64
          path: publish/RAMPilot.exe
